install_data('source_diodon.py',
    install_dir: join_paths(datadir, 'apport', 'package-hooks'))

install_data('diodon-crashdb.conf',
    install_dir: join_paths(etcdir, 'apport', 'crashdb.conf.d'))
